# Visual Almanac · 页面与交互设计

版本：2026-09-08，代码维护内容版。本文为设计规格，尚未生成页面或验证视觉效果。

## 1. 已确认范围

网站用于浏览风格、查找参考、复制配色与收藏。所有档案、图片、标签和笔记由源码中的内容文件维护，通过构建发布更新。页面不提供新增、编辑、删除、上传、回收站、内容导入导出或管理后台。

采用标准网站技术，完整源码可独立构建与部署，不依赖 GPT Website 或平台专属运行环境。第一版按只读内容和本机收藏设计，取消原方案中的 FastAPI、SQLite 和应用账号系统。跨设备收藏暂不实现。

## 2. 页面与导航

| 页面 | 路由 | 目的 |
| --- | --- | --- |
| 全部档案 | `/` | 浏览、搜索、筛选全部风格 |
| 我的收藏 | `/favorites` | 在已收藏档案中浏览与筛选 |
| 风格详情 | `/styles/:id` | 查看参考图、配色与设计笔记 |
| 未找到 | 未知页面或档案 ID | 说明链接无效并返回档案页 |

顶部导航只保留“Visual Almanac / 视觉风格档案馆”“全部档案”“我的收藏”。收藏入口显示有效收藏总数，不受当前筛选影响。页脚用一句话说明这是个人视觉参考档案，来源说明放在具体档案中。

收藏页复用档案列表页的布局与组件，只改变数据范围、标题和空状态。第一版不增加独立分类首页、配色库、设置页或统计看板。

## 3. 档案浏览页

### 3.1 布局顺序

桌面布局：顶部导航；正文左侧筛选栏；正文右侧依次为标题与总数、搜索与排序、已选条件、结果数量、卡片网格。海报从第一屏开始出现，不设置占满屏幕的宣传区。

建议初始尺寸：顶部高 64px，页面左右留白 32px，内容最大宽度 1600px，筛选栏宽 208px，栏间距 32px，卡片间距 24px。以 1440px 桌面为起点采用三列卡片。数值是排版起点，最终需实图验证。

### 3.2 筛选栏

| 维度 | 控件 | 首次展示 |
| --- | --- | --- |
| 分类 | 单选列表 | 全部、复古；有内容才显示新类别 |
| 情绪 | 多选标签 | 按配置顺序显示前 6 个，可展开其余 |
| 视觉特征 | 多选标签 | 高对比、低饱和、几何等实际存在的标签 |
| 色系 | 色点加文字的多选项 | 显示人工维护的色系，不从海报即时推断 |
| 应用场景 | 多选标签 | 仅展示档案中确有记录的建议用途 |

首次全部不选。分类只允许一个；同一标签维度内取并集，不同维度以及搜索之间取交集。标签选项来自内容字典与当前页面完整数据范围，不随每次筛选消失。收藏页只展示其收藏内容具有的选项，已选但暂时无匹配的条件仍保留在已选条中。

已选条每项可单独移除；“清除条件”同时清除搜索和所有筛选，保留当前页面与排序。清空收藏页条件不会转成全部档案。

### 3.3 搜索与排序

搜索占据工具栏主要宽度，提示语“搜索风格、别名或关键词”。搜索名称、别名、简介和标签名称。去除首尾空白、忽略拉丁字母大小写；多个空格分隔的关键词要求分别命中任一索引字段。第一版不做拼音、模糊纠错或语义检索。

输入停止约 200ms 后刷新结果，中文输入法合成过程中不筛选；回车立即提交，清除按钮立即清空。结果更新时不抢走焦点，不自动滚到页首。结果数量采用温和的读屏通知。

排序提供“档案顺序”“最近更新”“名称 A–Z”。默认使用内容中维护的 `order`；时间来自内容的显式 `updatedAt`，不使用构建时间；名称使用中文环境排序，相同值按稳定 ID 排序。收藏页额外提供“最近收藏”，并以此为默认。

### 3.4 卡片

卡片依次展示：完整封面、名称、最多两行简介、主配色的前 5 色、最多 3 个关键词。卡片不额外显示难以理解的内部编号。

封面位于 4:5 容器，使用 `object-fit: contain`，横图也完整显示；底色中性。图像和标题是进入详情的链接，收藏按钮是独立控件，避免将按钮嵌套在链接中。收藏按钮始终可见，触屏不依赖悬停。

列表色块仅用于快速判断，不提供复制动作；准确色值与说明在详情中查看。未核对配色不出现在卡片配色条。缺少配色时省略这一行，不填随机颜色。

悬停或键盘聚焦只改变边框/背景，不放大和裁切海报。名称完整显示，简介可以截断。首屏图片优先加载，其余懒加载，提前声明图片尺寸防止布局跳动。

### 3.5 数量增长

第一版每页 24 项，现有 12 项全部显示。超过 24 项才出现上一页、页码、下一页。更改搜索、筛选或排序回到第 1 页；内容减少使页码失效时修正到最后有效页。不用无限滚动，便于返回和定位。

## 4. 收藏交互

点击空心收藏图标后立即变为选中状态，提示“已收藏”；再次点击取消。使用 `aria-pressed` 和“收藏 ×× / 取消收藏 ××”标签。卡片和详情共享同一状态源，切换页面后不出现状态差异。

在收藏页取消收藏，项目立即退出列表，并显示约 5 秒的“已取消收藏 · 撤销”；撤销恢复原收藏时间和位置。将键盘焦点移到相邻项目，最后一个项目移除时移到空状态标题附近。普通列表和详情取消收藏后仍停留原处。

收藏只保存档案稳定 ID 与收藏时间，不复制档案内容。站点更新同一 ID 的配色或图片后，收藏自动显示新内容；改标题时不改 ID。下架档案的收藏 ID 暂时保留在存储中，但不计入列表与数量，日后恢复同一 ID 即可重新出现。

收藏页显示简短说明：“收藏保存在当前浏览器中。”无收藏时展示“还没有收藏的风格”和“浏览全部档案”；有收藏但筛选为空时展示“没有符合条件的收藏”和“清除条件”。

默认使用 localStorage，同一来源的正常浏览会话间可以保留数据。换浏览器、设备或站点来源不会自动同步；清理站点数据会移除收藏。[MDN localStorage](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)

封装存储异常与数据版本：读取损坏数据时不让页面崩溃，也不自动覆盖原始记录；当前页面使用内存状态，并说明“收藏暂时无法保存，关闭页面后可能丢失”。同源多标签页监听存储变化同步界面，并在写入前读取最新记录降低覆盖概率；第一版不承诺同时写入的冲突合并。

## 5. 风格详情页

### 5.1 信息顺序

页面顶部提供“返回档案”与分类路径。桌面主要区域左右分栏：左侧约 58% 为参考图与缩略图，右侧约 42% 为风格名称、简介、标签、收藏与主配色。下方依次显示设计特征、应用建议、实践笔记、参考来源和相关风格。长内容自然滚动，不把整段右栏固定在视口内。

每一栏只在数据存在时渲染；缺少字体记录就不显示字体栏目。分类与标签可点击，跳转到全部档案并应用对应单项筛选。不同用途的标签有明确文字，避免把室内参考图当作已经实践的网页方案。

返回优先恢复上一个列表入口、查询条件、页码和滚动位置；直接打开详情时回到全部档案页。浏览器前进/后退采用相同行为。

### 5.2 图片浏览

单张图片时不出现缩略图栏和左右箭头。多张时默认选封面，缩略图标记当前项，切换后更新图片说明与“2 / 4”计数。

点击大图进入全屏查看器。打开后焦点进入查看器，Tab 不离开弹层，Esc 或关闭按钮退出并恢复原焦点。左右键切换图片；放大/缩小与重置适配提供明确按钮，放大后可拖动查看。移动端保留按钮操作，不要求用户发现手势。海报始终保留原比例。

缩略图来自构建时生成的资源，详情加载较清晰版本，全屏按需加载原图。加载失败保留尺寸，显示“图片暂时无法加载”和重试，不影响文字与配色使用。

### 5.3 配色

每套配色显示名称、简短说明与色块列表。一套时无切换控件，多套时使用可换行的配色标签页。切换配色不影响当前图片与收藏。

每个色块配有色名、HEX、可选角色和来源说明，色值文字显示在中性底色上。点击“复制色值”复制 `#RRGGBB`；顶部提供“复制整组”和“复制 CSS”。整组按照维护顺序输出，以逗号分隔；CSS 使用稳定的配色 ID 与颜色 ID 组成变量名，不从中文名称临时生成。

核对状态规则：

- 图中标注已核对、人工设定：允许复制。
- 取样近似：允许复制，显示“近似取样”说明。
- 待核对：显示文本提示，不将待核对 HEX 作为可用色值；不参与卡片色条或批量复制。混合配色标明“仅复制已确认的 N 色”，没有可用颜色时禁用复制按钮。

复制成功给出短提示，不打开确认弹窗；失败时显示可选中的内容，允许手动复制。预览背景提供白、浅灰、深灰，仅作用于配色观察区域，不改变颜色本身。用量说明属于设计建议，不标成图片色彩统计结果。

### 5.4 笔记与相关风格

设计特征采用几个短段落：字体气质、布局、材质、视觉节奏。应用建议给出可执行描述，例如“大面积米白背景，酒红用于标题强调”，并区分整理建议与实际实践记录。所有内容在代码文件中维护。

相关风格最多 4 个：优先采用显式配置的关联 ID；不足时用共享标签数、同分类、内容顺序做确定性补齐，排除当前项和重复项。显示“同属复古”或“共同标签：木质”等真实关联理由，不宣称 AI 推荐。

## 6. URL 与状态约定

| 状态 | 保存位置 | 行为 |
| --- | --- | --- |
| 搜索、分类、标签、排序、页码 | URL 查询参数 | 刷新和分享保留条件 |
| 收藏 ID 与时间 | 收藏存储适配器 | 默认 localStorage，带 schemaVersion |
| 列表滚动与返回来源 | 当前标签页的导航状态 | 返回恢复，不写进收藏 |
| 当前图片、当前配色、全屏开关 | 详情页临时状态 | 新开详情恢复默认 |
| 配色预览背景 | 当前页面状态 | 默认浅灰 |

查询示例：`/?category=retro&mood=calm&mood=warm&sort=updated&page=1`。标签传稳定 ID。解析时对未知分类/标签、非法排序和页码进行规范化，避免页面报错。

搜索输入通过 replace 更新当前历史记录，避免每个字形成一条历史；明确点击分类、标签、排序、分页时写入导航历史，支持浏览器返回上一个筛选状态。页面切换和滚动恢复等待列表及图片占位布局建立后执行。

## 7. 响应式与视觉规则

| 屏幕范围 | 列表 | 筛选 | 详情 |
| --- | --- | --- | --- |
| ≥1200px | 3 列，宽屏最多 4 列且卡片不小于 240px | 左侧栏 | 图文双栏 |
| 768–1199px | 2 列 | 工具栏筛选按钮 | 单栏，图片设合理最大宽度 |
| <768px | 1 列，优先读清海报 | 底部筛选面板 | 图、标题、配色、笔记顺序排列 |

移动筛选面板使用临时选择，底部按钮“查看 N 个结果”应用并关闭；取消、Esc 或点击遮罩放弃未应用修改。“重置”只清空面板草稿，不立即改变背后列表。应用后查询状态与桌面一致。

视觉方向继续采用暖灰档案馆：浅色背景、深色正文、细线分区。颜色观察区域保持中性。标题字体与正文分工明确，不让装饰字体承载小字号操作文字。设计变量集中维护，包括背景、文字、强调色、间距、圆角、断点和动效。

建议最小触控区域 44×44px，正文从 16px 起，操作文字清楚可读。动效以 120–180ms 的颜色/透明度变化为主，尊重减少动态效果设置。以上是设计目标，开发时需验证实际对比度、焦点和布局。

## 8. 代码中的扩展与复用

以下是规划目录，尚未创建工程：

```text
src/
  app/                 路由、应用壳层、导航
  content/
    schema.ts          内容类型与运行时校验规则
    categories.ts      分类字典
    tags.ts            标签字典
    styles/            每个风格一个数据文件
    assets/            图片资源记录与来源
    index.ts           内容装载入口
  domain/
    catalog.ts         搜索、筛选、排序、分页
    related.ts         相关风格规则
    palette.ts         可复制颜色选择与输出格式
  repositories/
    catalog.ts         只读内容访问接口及静态实现
    favorites.ts       收藏访问接口及本地实现
  composables/
    useCatalogQuery.ts 查询参数与列表状态
    useFavorites.ts    收藏共享状态
    useClipboard.ts    复制与降级反馈
  components/
    catalog/           StyleCard、StyleGrid、FilterPanel、SearchToolbar
    palette/           PalettePanel、ColorSwatch
    media/             ReferenceGallery、ImageViewer
    common/            FavoriteButton、EmptyState、Toast
  pages/               CatalogPage、StyleDetailPage、NotFoundPage
  styles/              设计变量与基础样式
public/images/         发布用图片，使用站点相对路径
scripts/               内容校验与缩略图生成
docs/                  内容维护与独立部署说明
```

内容模型采用只读结构：Style 包含稳定 ID、名称、别名、简介、分类 ID、标签 ID、封面、参考图列表、配色列表、笔记、关联 ID、顺序和更新时间；收藏不属于 Style，独立保存。图片可被多份档案引用，图片说明可按档案单独记录。

每份配色和颜色具有稳定 ID。颜色包含可空 HEX、核对状态、色名和可选角色；待核对颜色可不提供 HEX。笔记优先使用结构化纯文本段落，避免为几条笔记引入任意 HTML 渲染。

页面只消费统一的档案模型，不直接导入某一个风格文件；内容入口自动收集内容目录中的模块并排序。新增风格通常只需增加内容文件和图片；新增分类/标签时补充字典。不要为特定风格编写单独页面或分支条件。

收藏访问接口至少提供读取、切换和订阅，暴露是否持久化成功；组件不直接调用 localStorage。将来确需跨设备收藏时替换为 API 实现，届时再增加账号及服务端。只读内容接口统一提供列表和按 ID 读取，不为未来假设实现空后台。

筛选、排序、配色格式化和相关规则写成纯函数。组件通过属性和事件组合；共同页面壳层可复用，内容差异由数据提供。保持抽象与实际复用点一致，不建立复杂插件系统或通用 CMS。

## 9. 内容更新与独立部署

维护流程：添加图片 → 在内容目录增加或修改风格文件 → 运行内容校验 → 生成缩略图并构建 → 本地预览 → 部署构建产物。发布前检查 ID 唯一、关联有效、图片存在、日期格式、HEX 与核对状态一致；错误阻止发布，说明具体内容文件和字段。

首批 12 张海报保留原文件，可在实施时复制成稳定英文资源名并记录映射；不把时间戳文件名当成档案 ID。维护说明提供一份最小档案模板和一份多图、多配色示例。

第一版推荐 Vue 3 + TypeScript + Vite，构建产物由 Nginx 或标准静态服务器部署。Vite 官方说明可独立部署构建目录，且预览服务不用于生产。[Vite 部署文档](https://vite.dev/guide/static-deploy.html)

独立交付包括源码、依赖锁定、构建/校验命令、Nginx 配置、可选 Dockerfile 与 Compose、中文更新和回滚说明。配置详情路由的 HTML 回退，但缺失图片应返回真实 404；支持配置站点基础路径，避免写死域名和 Windows 路径。入口 HTML 与内容索引及时更新，带内容摘要的资源可长期缓存，部署时整批替换产物以免新旧引用混用。

静态构建中的内容均会交付给访问者。若档案需要私人访问，在部署入口对 HTML、数据和图片整体设置访问控制；前端隐藏链接不能作为权限措施。站点是否公开留到部署前确定，不默认公开发布。

内容备份与回滚使用源码、原始素材和构建产物的版本管理；不提供网站内管理入口。收藏是浏览器用户状态，不随代码版本回滚，也不包含在服务器内容备份中。

## 10. 验收场景

| 场景 | 预期结果 |
| --- | --- |
| 从首页进入任一档案并返回 | 恢复查询、页码和滚动位置 |
| 输入中文名称、别名与多个关键词 | 合成输入不闪动，匹配规则一致 |
| 同维度选两个标签，再选另一维度 | 先并集再交集，条件可逐项移除 |
| 刷新或分享带筛选参数的地址 | 得到相同内容范围；收藏范围仍由本浏览器决定 |
| 收藏后刷新；在卡片与详情间切换 | 收藏状态一致且保留 |
| 收藏页取消并撤销 | 项目移除后可恢复位置，焦点不丢失 |
| 浏览器禁止存储 | 页面仍可用，明确告知收藏未持久化 |
| 复制待核对与已确认混合配色 | 仅输出可用色，数量与提示一致 |
| 图片加载失败、档案不存在 | 有明确恢复路径，页面不崩溃 |
| 手机筛选后取消，再次打开 | 已应用条件不被未提交草稿改变 |
| 只增加内容文件和图片 | 无需修改页面组件即可显示新档案 |
| 内容 ID 重复、引用图片缺失 | 校验阻止构建并定位问题 |
| 独立静态部署后直接刷新详情地址 | 页面正常，图片缺失不会错误返回首页 HTML |

当前完成的是内容组织、页面结构、交互规则与工程边界。下一阶段以浏览页和详情页为对象确定视觉稿，再按本规格实现和验收。
